*.pyc
__pycache__/
*.egg-info/
.venv/
screenshots/
build/
dist/

config.yaml

# User data directory (config with secrets, sessions, screenshots, logs).
# The knowledge/ subdirectory is EXCEPTION: it is project-shared and committable
# (corrections / success_paths / kb). Ignore contents, then re-include knowledge.
.enikk-home/*
!.enikk-home/knowledge/

# Runtime logs & local artifacts
*.log
weights/
enikk-run*.out.log
enikk-run*.err.log
pip-install*.log

# Data files
accounts/
auth.json
auth.lock
logs/
memories/
sessions/
trajectories/
models_dev_cache.json
SOUL.md
view.py
failed_trajectories.jsonl

# IDE & Editor
.vscode/
.idea/

# Claude Code
.claude/*
!.claude/commands/

# Local one-off verification scripts (may contain real credentials — never commit)
_live_*.py
_smoke_*.py
_move_*.ps1

# Unrelated third-party dirs accidentally cloned here (each has its own .git)
.tmp-archify/
.tmp-jev/
report/

# Local secrets (never commit)
.env.local
.env

# Runtime-generated artifacts (login states / dynamic accounts — never commit)
.auth/
.runtime/
_shots/

# Large installers/clients downloaded during runs (hundreds of MB — never commit)
downloads/
