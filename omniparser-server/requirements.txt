# Core (CPU works out of the box; GPU strongly recommended for latency)
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
numpy>=1.26,<2.1
opencv-python-headless>=4.9
ultralytics>=8.2
rapidocr-onnxruntime>=1.3
huggingface_hub>=0.23

# Icon captioning (optional — ENABLE_CAPTION=1, GPU recommended)
# torch + torchvision (CUDA build) and transformers:
#   transformers>=4.44
# Install torch separately per https://pytorch.org/get-started/locally/
